<!-- Drops Management -->
<div class="space-y-6">
    <!-- Responsive Header -->
    <div class="responsive-header bg-white rounded-lg shadow-sm border border-gray-200 p-4 sm:p-6">
        <!-- Mobile Layout (320px-767px) -->
        <div class="flex flex-col space-y-4 sm:hidden">
            <!-- Title Row -->
            <div>
                <h1 class="text-xl font-bold text-gray-900 leading-tight">Drops</h1>
                <p class="mt-1 text-sm text-gray-500">Manage your drops and track fan engagement</p>
            </div>

            <!-- Action Button -->
            <div>
                <button data-action="create-drop" class="create-drop-btn inline-flex items-center justify-center w-full px-4 py-2.5 border border-transparent shadow-sm text-sm font-medium rounded-lg text-white bg-primary-600 hover:bg-primary-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary-500 transition-colors duration-200 min-h-[44px]">
                    <svg class="-ml-0.5 mr-2 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6" />
                    </svg>
                    Create Drop
                </button>
            </div>
        </div>

        <!-- Tablet Layout (768px-1023px) -->
        <div class="hidden sm:flex lg:hidden items-center justify-between">
            <div>
                <h1 class="text-2xl font-bold text-gray-900">Drops</h1>
                <p class="mt-1 text-sm text-gray-500">Manage your drops and track fan engagement</p>
            </div>

            <div>
                <button data-action="create-drop" class="create-drop-btn inline-flex items-center px-4 py-2.5 border border-transparent shadow-sm text-sm font-medium rounded-lg text-white bg-primary-600 hover:bg-primary-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary-500 transition-colors duration-200 min-h-[44px]">
                    <svg class="-ml-0.5 mr-2 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6" />
                    </svg>
                    Create Drop
                </button>
            </div>
        </div>

        <!-- Desktop Layout (1024px+) -->
        <div class="hidden lg:flex items-center justify-between">
            <div>
                <h1 class="text-3xl font-bold text-gray-900">Drops</h1>
                <p class="mt-1 text-base text-gray-500">Manage your drops and track fan engagement</p>
            </div>

            <div>
                <button data-action="create-drop" class="create-drop-btn inline-flex items-center px-6 py-3 border border-transparent shadow-sm text-sm font-medium rounded-lg text-white bg-primary-600 hover:bg-primary-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary-500 transition-colors duration-200">
                    <svg class="-ml-0.5 mr-2 h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6" />
                    </svg>
                    Create Drop
                </button>
            </div>
        </div>
    </div>

    <!-- Consolidated Stats Card -->
    <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
        <div class="flex items-center justify-between mb-4">
            <h3 class="text-lg font-semibold text-gray-900">Overview</h3>
            <div class="w-8 h-8 bg-gradient-to-br from-primary-500 to-primary-600 rounded-lg flex items-center justify-center">
                <svg class="w-4 h-4 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
                </svg>
            </div>
        </div>

        <div class="grid grid-cols-2 gap-6">
            <!-- Total Drops -->
            <div class="text-center p-4 bg-gray-50 rounded-lg">
                <div class="text-2xl font-bold text-gray-900 mb-1">{{stats.totalDrops}}</div>
                <div class="text-sm font-medium text-gray-500">Total Drops</div>
            </div>

            <!-- Active Drops -->
            <div class="text-center p-4 bg-emerald-50 rounded-lg">
                <div class="text-2xl font-bold text-emerald-700 mb-1">{{stats.activeDrops}}</div>
                <div class="text-sm font-medium text-emerald-600">Active Drops</div>
            </div>
        </div>
    </div>

    <!-- Professional Drops List - Fixed Alignment -->
    <div class="bg-white rounded-xl shadow-sm border border-gray-200/60">
        {{#if drops.length}}
        <div class="p-6 space-y-6">
            {{#each drops}}
            <!-- Professional Drop Card - Perfect Alignment -->
            <div class="drop-card-modern group relative bg-white border border-gray-200/60 rounded-xl shadow-sm hover:shadow-lg hover:shadow-gray-900/10 hover:border-gray-300/80 transition-all duration-300 ease-out overflow-hidden w-full">

                    <!-- Card Header - Perfect Visual Hierarchy -->
                    <div class="px-6 pt-6 pb-4">
                        <div class="flex items-start gap-4">
                            <!-- Premium Drop Visual - Larger Impact -->
                            <div class="flex-shrink-0">
                                {{#if this.cover_image}}
                                    <div class="relative">
                                        <img class="h-16 w-16 rounded-xl object-cover shadow-sm ring-1 ring-gray-200/50" src="{{this.cover_image}}" alt="{{this.title}}">
                                        <div class="absolute inset-0 rounded-xl ring-1 ring-inset ring-gray-900/10"></div>
                                    </div>
                                {{else}}
                                    <div class="h-16 w-16 rounded-xl bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50 flex items-center justify-center shadow-sm ring-1 ring-gray-200/50">
                                        <svg class="h-8 w-8 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                                            <path stroke-linecap="round" stroke-linejoin="round" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
                                        </svg>
                                    </div>
                                {{/if}}
                            </div>

                            <!-- Content Area - Enhanced Typography -->
                            <div class="flex-1 min-w-0">
                                <!-- Title and Status - Perfect Balance -->
                                <div class="flex items-start justify-between gap-4 mb-3">
                                    <div class="flex-1 min-w-0">
                                        <h3 class="text-xl font-semibold text-gray-900 leading-7 truncate mb-1">{{this.title}}</h3>
                                        {{#if this.description}}
                                        <p class="text-sm text-gray-600 leading-5 line-clamp-2">{{this.description}}</p>
                                        {{/if}}
                                    </div>
                                    <div class="flex-shrink-0">
                                        <span class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full text-sm font-medium {{#if this.is_active}}bg-emerald-50 text-emerald-700 ring-1 ring-emerald-600/20{{else}}bg-gray-50 text-gray-600 ring-1 ring-gray-200{{/if}}">
                                            <span class="w-2 h-2 rounded-full {{#if this.is_active}}bg-emerald-500{{else}}bg-gray-400{{/if}}"></span>
                                            {{#if this.is_active}}Active{{else}}Inactive{{/if}}
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Stats Section - Clean Information Display -->
                    <div class="px-6 pb-6">
                        <div class="flex items-center gap-8 text-sm">
                            <div class="flex items-center gap-2 text-gray-600">
                                <div class="flex items-center justify-center w-5 h-5 rounded-full bg-blue-50">
                                    <svg class="h-3 w-3 text-blue-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M15 19.128a9.38 9.38 0 002.625.372 9.337 9.337 0 004.121-.952 4.125 4.125 0 00-7.533-2.493M15 19.128v-.003c0-1.113-.285-2.16-.786-3.07M15 19.128v.106A12.318 12.318 0 018.624 21c-2.331 0-4.512-.645-6.374-1.766l-.001-.109a6.375 6.375 0 0111.964-3.07M12 6.375a3.375 3.375 0 11-6.75 0 3.375 3.375 0 016.75 0zm8.25 2.25a2.625 2.625 0 11-5.25 0 2.625 2.625 0 015.25 0z" />
                                    </svg>
                                </div>
                                <span class="font-medium text-gray-900">{{this.signup_count}}</span>
                                <span class="text-gray-500">signups</span>
                            </div>
                            <div class="flex items-center gap-2 text-gray-600">
                                <div class="flex items-center justify-center w-5 h-5 rounded-full bg-gray-50">
                                    <svg class="h-3 w-3 text-gray-600" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M6.75 3v2.25M17.25 3v2.25M3 18.75V7.5a2.25 2.25 0 012.25-2.25h13.5A2.25 2.25 0 0121 7.5v11.25m-18 0A2.25 2.25 0 005.25 21h13.5a2.25 2.25 0 002.25-2.25m-18 0v-7.5A2.25 2.25 0 015.25 9h13.5a2.25 2.25 0 012.25 2.25v7.5" />
                                    </svg>
                                </div>
                                <span class="text-gray-500">{{formatDate this.created_at "MMM dd, yyyy"}}</span>
                            </div>
                        </div>
                    </div>

                    <!-- Professional Action Bar - Modern Button Design -->
                    <div class="border-t border-gray-100/80 bg-gray-50/30 px-6 py-5">
                        <!-- Mobile Layout (up to 640px) - Optimized 2x2 Grid -->
                        <div class="grid grid-cols-2 gap-3 sm:hidden">
                            <a href="/drop/{{this.slug}}" target="_blank" class="btn-modern btn-primary-modern">
                                <svg class="h-4 w-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 6H5.25A2.25 2.25 0 003 8.25v10.5A2.25 2.25 0 005.25 21h10.5A2.25 2.25 0 0018 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" />
                                </svg>
                                <span class="truncate font-medium">View</span>
                            </a>

                            <button onclick="copyDropLink('{{../domain}}/drop/{{this.slug}}', this)" class="btn-modern btn-secondary-modern">
                                <svg class="h-4 w-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M15.666 3.888A2.25 2.25 0 0013.5 2.25h-3c-1.03 0-1.9.693-2.166 1.638m7.332 0c.055.194.084.4.084.612v0a.75.75 0 01-.75.75H9a.75.75 0 01-.75-.75v0c0-.212.03-.418.084-.612m7.332 0c.646.049 1.288.11 1.927.184 1.1.128 1.907 1.077 1.907 2.185V19.5a2.25 2.25 0 01-2.25 2.25H6.75A2.25 2.25 0 014.5 19.5V6.257c0-1.108.806-2.057 1.907-2.185a48.208 48.208 0 011.927-.184" />
                                </svg>
                                <span class="copy-text truncate font-medium">Copy</span>
                            </button>

                            <a href="/drops/{{this.id}}/edit" class="btn-modern btn-secondary-modern">
                                <svg class="h-4 w-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M16.862 4.487l1.687-1.688a1.875 1.875 0 112.652 2.652L6.832 19.82a4.5 4.5 0 01-1.897 1.13l-2.685.8.8-2.685a4.5 4.5 0 011.13-1.897L16.863 4.487zm0 0L19.5 7.125" />
                                </svg>
                                <span class="truncate font-medium">Edit</span>
                            </a>

                            <button data-action="delete-drop" data-drop-id="{{this.id}}" data-drop-title="{{this.title}}" class="delete-drop-btn btn-modern btn-danger-modern">
                                <svg class="h-4 w-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                                    <path stroke-linecap="round" stroke-linejoin="round" d="M14.74 9l-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 01-2.244 2.077H8.084a2.25 2.25 0 01-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 00-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 013.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 00-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 00-7.5 0" />
                                </svg>
                                <span class="truncate font-medium">Delete</span>
                            </button>
                        </div>

                        <!-- Desktop Layout (640px+) - Professional Horizontal Layout -->
                        <div class="hidden sm:flex items-center justify-between">
                            <!-- Primary Actions Group -->
                            <div class="flex items-center gap-3">
                                <a href="/drop/{{this.slug}}" target="_blank" class="btn-modern btn-primary-modern">
                                    <svg class="h-4 w-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 6H5.25A2.25 2.25 0 003 8.25v10.5A2.25 2.25 0 005.25 21h10.5A2.25 2.25 0 0018 18.75V10.5m-10.5 6L21 3m0 0h-5.25M21 3v5.25" />
                                    </svg>
                                    <span class="font-medium">View Live</span>
                                </a>

                                <button onclick="copyDropLink('{{../domain}}/drop/{{this.slug}}', this)" class="btn-modern btn-secondary-modern">
                                    <svg class="h-4 w-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M15.666 3.888A2.25 2.25 0 0013.5 2.25h-3c-1.03 0-1.9.693-2.166 1.638m7.332 0c.055.194.084.4.084.612v0a.75.75 0 01-.75.75H9a.75.75 0 01-.75-.75v0c0-.212.03-.418.084-.612m7.332 0c.646.049 1.288.11 1.927.184 1.1.128 1.907 1.077 1.907 2.185V19.5a2.25 2.25 0 01-2.25 2.25H6.75A2.25 2.25 0 014.5 19.5V6.257c0-1.108.806-2.057 1.907-2.185a48.208 48.208 0 011.927-.184" />
                                    </svg>
                                    <span class="copy-text font-medium">Copy Link</span>
                                </button>
                            </div>

                            <!-- Secondary Actions Group -->
                            <div class="flex items-center gap-3">
                                <a href="/drops/{{this.id}}/edit" class="btn-modern btn-secondary-modern">
                                    <svg class="h-4 w-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M16.862 4.487l1.687-1.688a1.875 1.875 0 112.652 2.652L6.832 19.82a4.5 4.5 0 01-1.897 1.13l-2.685.8.8-2.685a4.5 4.5 0 011.13-1.897L16.863 4.487zm0 0L19.5 7.125" />
                                    </svg>
                                    <span class="font-medium">Edit</span>
                                </a>

                                <button data-action="delete-drop" data-drop-id="{{this.id}}" data-drop-title="{{this.title}}" class="delete-drop-btn btn-modern btn-danger-modern">
                                    <svg class="h-4 w-4 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
                                        <path stroke-linecap="round" stroke-linejoin="round" d="M14.74 9l-.346 9m-4.788 0L9.26 9m9.968-3.21c.342.052.682.107 1.022.166m-1.022-.165L18.16 19.673a2.25 2.25 0 01-2.244 2.077H8.084a2.25 2.25 0 01-2.244-2.077L4.772 5.79m14.456 0a48.108 48.108 0 00-3.478-.397m-12 .562c.34-.059.68-.114 1.022-.165m0 0a48.11 48.11 0 013.478-.397m7.5 0v-.916c0-1.18-.91-2.164-2.09-2.201a51.964 51.964 0 00-3.32 0c-1.18.037-2.09 1.022-2.09 2.201v.916m7.5 0a48.667 48.667 0 00-7.5 0" />
                                    </svg>
                                    <span class="font-medium">Delete</span>
                                </button>
                            </div>
                        </div>
                    </div>
            </div>
            {{/each}}
            {{else}}
            <!-- Empty State -->
            <div class="text-center py-12">
                <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10" />
                </svg>
                <h3 class="mt-2 text-sm font-medium text-gray-900">No drops yet</h3>
                <p class="mt-1 text-sm text-gray-500">Get started by creating your first drop to engage with your fans.</p>
                <div class="mt-6">
                    <button data-action="create-drop" class="create-drop-btn inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-primary-600 hover:bg-primary-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-primary-500">
                        <svg class="-ml-1 mr-2 h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6" />
                        </svg>
                        Create Your First Drop
                    </button>
                </div>
            </div>
            {{/if}}
        </div>
    </div>
</div>

<!-- Modern Delete Confirmation Modal with Glassmorphism -->
<div id="deleteModal" class="fixed inset-0 bg-black bg-opacity-50 backdrop-blur-sm overflow-y-auto h-full w-full hidden z-50 flex items-center justify-center p-4">
    <div class="relative w-full max-w-md mx-auto">
        <!-- Glassmorphism Modal -->
        <div class="bg-white bg-opacity-95 backdrop-blur-xl rounded-2xl shadow-2xl border border-white border-opacity-20 p-6 sm:p-8">
            <!-- Header with Warning Icon -->
            <div class="flex items-center justify-center mb-6">
                <div class="w-16 h-16 bg-gradient-to-br from-red-100 to-red-200 rounded-full flex items-center justify-center shadow-lg">
                    <svg class="w-8 h-8 text-red-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z" />
                    </svg>
                </div>
            </div>

            <!-- Content -->
            <div class="text-center mb-8">
                <h3 class="text-xl font-semibold text-gray-900 mb-3">Delete Drop?</h3>
                <p class="text-gray-600 leading-relaxed">
                    Are you sure you want to delete
                    <span class="font-semibold text-gray-900">"<span id="dropTitle"></span>"</span>?
                </p>
                <p class="text-sm text-red-600 mt-2 font-medium">
                    ⚠️ This action cannot be undone
                </p>
            </div>

            <!-- Action Buttons -->
            <div class="flex flex-col sm:flex-row gap-3">
                <button id="cancelDelete"
                        class="flex-1 px-6 py-3 border border-gray-300 text-gray-700 text-sm font-medium rounded-lg hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-gray-300 transition-colors duration-200">
                    Cancel
                </button>
                <button id="confirmDelete"
                        class="flex-1 px-6 py-3 bg-red-600 text-white text-sm font-medium rounded-lg hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-500 transition-colors duration-200 shadow-sm">
                    <span class="flex items-center justify-center">
                        <svg class="-ml-0.5 mr-2 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" />
                        </svg>
                        Delete Drop
                    </span>
                </button>
            </div>
        </div>
    </div>
</div>



<!-- Drops JavaScript -->
<script>
let dropToDelete = null;

// Create drop function
function createDrop() {
    console.log('Create drop button clicked');

    const modal = document.getElementById('create-drop-dialog');
    if (modal) {
        modal.classList.remove('hidden');

        // Focus on the title input
        setTimeout(() => {
            const titleInput = document.getElementById('drop-title');
            if (titleInput) {
                titleInput.focus();
            }
        }, 100);
    } else {
        window.location.href = '/#create-drop';
    }
}

// Copy drop link to clipboard with visual feedback
async function copyDropLink(url, buttonElement) {
    try {
        await navigator.clipboard.writeText(url);

        // Visual feedback - change button text temporarily
        const textElement = buttonElement.querySelector('.copy-text');
        const originalText = textElement.textContent;

        textElement.textContent = 'Copied!';
        buttonElement.classList.add('bg-green-50', 'border-green-300', 'text-green-700');
        buttonElement.classList.remove('bg-white', 'border-gray-300', 'text-gray-700');

        // Reset after 2 seconds
        setTimeout(() => {
            textElement.textContent = originalText;
            buttonElement.classList.remove('bg-green-50', 'border-green-300', 'text-green-700');
            buttonElement.classList.add('bg-white', 'border-gray-300', 'text-gray-700');
        }, 2000);

        // Show toast notification if available
        if (typeof showToast === 'function') {
            showToast('Drop link copied to clipboard!', 'success');
        }
    } catch (err) {
        console.error('Failed to copy link:', err);

        // Fallback for older browsers
        const textArea = document.createElement('textarea');
        textArea.value = url;
        document.body.appendChild(textArea);
        textArea.select();
        document.execCommand('copy');
        document.body.removeChild(textArea);

        if (typeof showToast === 'function') {
            showToast('Drop link copied to clipboard!', 'success');
        }
    }
}

// Delete drop function
function deleteDrop(dropId, dropTitle) {
    console.log('Delete drop clicked:', dropId, dropTitle);
    dropToDelete = dropId;

    const titleElement = document.getElementById('dropTitle');
    const modalElement = document.getElementById('deleteModal');

    if (titleElement) {
        titleElement.textContent = dropTitle;
    }

    if (modalElement) {
        modalElement.classList.remove('hidden');
    }
}

// Simple event handlers setup
function setupEventHandlers() {
    console.log('Setting up event handlers');

    // Create Drop button handlers
    const createDropButtons = document.querySelectorAll('.create-drop-btn, [data-action="create-drop"]');
    console.log('Found create drop buttons:', createDropButtons.length);

    createDropButtons.forEach((button) => {
        button.addEventListener('click', function(e) {
            e.preventDefault();
            console.log('Create drop button clicked');
            createDrop();
        });
    });

    // Delete Drop button handlers
    const deleteDropButtons = document.querySelectorAll('.delete-drop-btn, [data-action="delete-drop"]');
    console.log('Found delete drop buttons:', deleteDropButtons.length);

    deleteDropButtons.forEach((button) => {
        const dropId = button.getAttribute('data-drop-id');
        const dropTitle = button.getAttribute('data-drop-title');

        button.addEventListener('click', function(e) {
            e.preventDefault();
            console.log('Delete drop button clicked:', dropId, dropTitle);
            deleteDrop(dropId, dropTitle);
        });
    });

    console.log('Event handlers setup complete');
}

// Initialize event handlers when DOM is ready
document.addEventListener('DOMContentLoaded', function() {
    console.log('DOM Content Loaded');
    setupEventHandlers();
});

// Also setup handlers if DOM is already loaded
if (document.readyState === 'loading') {
    console.log('DOM still loading, waiting for DOMContentLoaded');
} else {
    console.log('DOM already loaded, setting up handlers immediately');
    setupEventHandlers();
}

// Confirm delete handler
document.getElementById('confirmDelete').addEventListener('click', async function() {
    console.log('Confirm delete clicked:', dropToDelete);

    if (!dropToDelete) {
        console.log('No drop to delete');
        return;
    }

    try {
        console.log('Sending DELETE request to:', `/api/drops/${dropToDelete}`);

        const response = await fetch(`/api/drops/${dropToDelete}`, {
            method: 'DELETE',
            headers: {
                'Content-Type': 'application/json'
            }
        });

        console.log('Response status:', response.status);

        if (response.ok) {
            const result = await response.json();
            console.log('Delete successful:', result);
            showToast('Drop deleted successfully!', 'success');
            // Reload the page to update the list
            setTimeout(() => {
                window.location.reload();
            }, 1000);
        } else {
            const error = await response.json();
            console.log('Delete failed:', error);
            showToast(error.message || 'Failed to delete drop', 'error');
        }
    } catch (error) {
        console.log('Delete error:', error);
        showToast('Failed to delete drop', 'error');
    }

    document.getElementById('deleteModal').classList.add('hidden');
    dropToDelete = null;
});

// Cancel delete handler
document.getElementById('cancelDelete').addEventListener('click', function() {
    console.log('Delete cancelled');
    document.getElementById('deleteModal').classList.add('hidden');
    dropToDelete = null;
});

// Close modal when clicking outside
document.getElementById('deleteModal').addEventListener('click', function(e) {
    if (e.target === this) {
        console.log('Modal closed by clicking outside');
        this.classList.add('hidden');
        dropToDelete = null;
    }
});

// Close modal with Escape key
document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
        const modal = document.getElementById('deleteModal');
        if (modal && !modal.classList.contains('hidden')) {
            console.log('Modal closed with Escape key');
            modal.classList.add('hidden');
            dropToDelete = null;
        }
    }
});

// Toast notification function
function showToast(message, type) {
    const toast = document.createElement('div');
    toast.className = `fixed top-4 right-4 px-4 py-2 rounded-md shadow-lg z-50 ${
        type === 'success' ? 'bg-green-500 text-white' : 'bg-red-500 text-white'
    }`;
    toast.textContent = message;
    document.body.appendChild(toast);

    setTimeout(() => {
        document.body.removeChild(toast);
    }, 3000);
}

// Note: Using local createDrop function instead of main.js to avoid conflicts
</script>

<style>
/* Enhanced Drops Page Styling */

/* Smooth status indicator animation */
@keyframes pulse-gentle {
    0%, 100% {
        opacity: 1;
    }
    50% {
        opacity: 0.7;
    }
}

.animate-pulse {
    animation: pulse-gentle 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}

/* Enhanced focus states for accessibility */
button:focus-visible,
a:focus-visible {
    outline: 2px solid #3b82f6;
    outline-offset: 2px;
}

/* Smooth transitions for responsive layout changes */
.responsive-header {
    transition: all 0.3s ease-in-out;
}

/* Line clamp utilities for text truncation */
.line-clamp-1 {
    overflow: hidden;
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 1;
}

.line-clamp-2 {
    overflow: hidden;
    display: -webkit-box;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
}

/* Enhanced card hover effects */
.group:hover .group-hover\:shadow-lg {
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
}

/* Mobile-first responsive grid improvements */
@media (max-width: 640px) {
    .grid-cols-2 {
        gap: 0.5rem;
    }

    .space-y-6 > * + * {
        margin-top: 1rem;
    }
}

/* Tablet optimizations */
@media (min-width: 768px) and (max-width: 1023px) {
    .space-y-6 > * + * {
        margin-top: 1.5rem;
    }
}

/* Desktop enhancements */
@media (min-width: 1024px) {
    .space-y-6 > * + * {
        margin-top: 2rem;
    }
}

/* Modal animations and enhancements */
#deleteModal {
    animation: fadeIn 0.2s ease-out;
}

#deleteModal.hidden {
    animation: fadeOut 0.2s ease-in;
}

#create-drop-dialog {
    animation: fadeIn 0.2s ease-out;
}

#create-drop-dialog.hidden {
    animation: fadeOut 0.2s ease-in;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: scale(0.95);
    }
    to {
        opacity: 1;
        transform: scale(1);
    }
}

@keyframes fadeOut {
    from {
        opacity: 1;
        transform: scale(1);
    }
    to {
        opacity: 0;
        transform: scale(0.95);
    }
}

/* Enhanced button hover effects */
button:hover {
    transform: translateY(-1px);
    transition: all 0.2s ease-in-out;
}

button:active {
    transform: translateY(0);
}

/* Improved focus accessibility */
button:focus-visible,
a:focus-visible {
    outline: 2px solid #3b82f6;
    outline-offset: 2px;
    border-radius: 8px;
}

/* Mobile touch improvements */
@media (hover: none) and (pointer: coarse) {
    /* Mobile devices - improve touch targets */
    button, a {
        min-height: 44px;
        min-width: 44px;
    }

    /* Remove hover effects on touch devices */
    button:hover,
    a:hover {
        transform: none;
    }

    /* Add touch feedback */
    button:active,
    a:active {
        transform: scale(0.98);
        transition: transform 0.1s ease-in-out;
    }
}

/* iOS Safari specific fixes - CRITICAL: cursor pointer fixes click events */
button {
    -webkit-appearance: none;
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
    cursor: pointer; /* This fixes iOS Safari click event bug */
}

/* Ensure all clickable elements have cursor pointer for iOS Safari */
.create-drop-btn,
.delete-drop-btn,
button,
[data-action] {
    cursor: pointer !important;
}

/* Improved Button System - Better Padding and Alignment */
.btn-improved {
    @apply inline-flex items-center justify-center gap-2.5 px-4 py-2.5 text-sm font-medium rounded-lg;
    @apply transition-all duration-200 ease-out cursor-pointer;
    @apply focus:outline-none focus:ring-2 focus:ring-offset-2;
    @apply disabled:opacity-50 disabled:cursor-not-allowed;
    @apply min-h-[44px] min-w-[80px]; /* Consistent sizing */
    @apply whitespace-nowrap; /* Prevent text wrapping */
}

.btn-primary {
    @apply bg-blue-600 text-white border border-blue-600;
    @apply hover:bg-blue-700 hover:border-blue-700;
    @apply focus:ring-blue-500;
    @apply shadow-sm hover:shadow-md;
}

.btn-secondary {
    @apply bg-white text-gray-700 border border-gray-300;
    @apply hover:bg-gray-50 hover:border-gray-400 hover:text-gray-800;
    @apply focus:ring-blue-500;
    @apply shadow-sm hover:shadow-md;
}

.btn-danger {
    @apply bg-white text-red-700 border border-red-300;
    @apply hover:bg-red-50 hover:border-red-400 hover:text-red-800;
    @apply focus:ring-red-500;
    @apply shadow-sm hover:shadow-md;
}

/* Mobile optimizations - Better spacing */
@media (max-width: 640px) {
    .btn-improved {
        @apply px-3 py-2.5 text-sm gap-2;
        @apply min-h-[44px] min-w-[70px]; /* Maintain accessibility and consistency */
    }
}

/* Desktop refinements */
@media (min-width: 1024px) {
    .btn-improved {
        @apply px-5 py-3 gap-3; /* More generous spacing on large screens */
    }
}

/* Professional Card System - Premium B2B Design */
.drop-card-modern {
    @apply transition-all duration-300 ease-out;
    @apply hover:shadow-xl hover:shadow-gray-900/15;
    @apply hover:-translate-y-1 hover:scale-[1.01];
    @apply w-full block;
    @apply cursor-pointer;
    /* Enhanced shadow and border for premium feel */
    box-shadow: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
}

.drop-card-modern:hover .btn-modern {
    @apply shadow-lg transform scale-105;
}

/* Perfect Card Spacing - Research-Based 8px Grid */
.drop-card-modern {
    @apply mb-0; /* Remove any default margins */
}

/* Professional Typography Hierarchy - Material Design 3 Inspired */
.drop-card-modern h3 {
    @apply text-xl font-semibold leading-7; /* 28px line height = 3.5 * 8px */
    @apply text-gray-900 m-0;
    @apply tracking-tight; /* Tighter letter spacing for premium feel */
}

.drop-card-modern p {
    @apply text-sm leading-5; /* 20px line height = 2.5 * 8px */
    @apply text-gray-600 m-0;
    @apply tracking-normal;
}

/* Card Content Perfect Alignment */
.drop-card-modern > div {
    @apply w-full;
}

/* Container Spacing - Perfect Alignment System */
.space-y-6 > * + * {
    @apply mt-6; /* 24px = 3 * 8px for consistent card spacing */
}

/* Card Container Alignment Fix */
.drop-card-modern {
    @apply w-full; /* Ensure full width */
    @apply mx-0; /* Remove any horizontal margins */
    @apply block; /* Block display for consistent alignment */
}

/* Modern Button System - Professional Design */
.btn-modern {
    @apply inline-flex items-center justify-center gap-2;
    @apply px-4 py-2.5 rounded-lg text-sm font-medium;
    @apply transition-all duration-200 ease-out;
    @apply focus:outline-none focus:ring-2 focus:ring-offset-2;
    @apply cursor-pointer;
    @apply min-h-[44px]; /* Accessibility touch target */
    @apply border border-transparent;
}

/* Primary Button - Premium Blue */
.btn-primary-modern {
    @apply bg-blue-600 text-white;
    @apply hover:bg-blue-700 hover:shadow-lg hover:shadow-blue-500/25;
    @apply focus:ring-blue-500;
    @apply active:bg-blue-800 active:scale-95;
}

/* Secondary Button - Elegant Gray */
.btn-secondary-modern {
    @apply bg-white text-gray-700 border-gray-200;
    @apply hover:bg-gray-50 hover:border-gray-300 hover:shadow-md;
    @apply focus:ring-gray-500;
    @apply active:bg-gray-100 active:scale-95;
}

/* Danger Button - Professional Red */
.btn-danger-modern {
    @apply bg-white text-red-600 border-red-200;
    @apply hover:bg-red-50 hover:border-red-300 hover:shadow-md hover:shadow-red-500/10;
    @apply focus:ring-red-500;
    @apply active:bg-red-100 active:scale-95;
}

/* Perfect Alignment System - Consistent Padding */
.drop-card-modern .px-6 {
    padding-left: 1.5rem !important; /* 24px = 3 * 8px */
    padding-right: 1.5rem !important; /* 24px = 3 * 8px */
}

.drop-card-modern .pt-6 {
    padding-top: 1.5rem !important; /* 24px = 3 * 8px */
}

.drop-card-modern .pb-4 {
    padding-bottom: 1rem !important; /* 16px = 2 * 8px */
}

.drop-card-modern .pb-6 {
    padding-bottom: 1.5rem !important; /* 24px = 3 * 8px */
}

.drop-card-modern .py-5 {
    padding-top: 1.25rem !important; /* 20px = 2.5 * 8px */
    padding-bottom: 1.25rem !important; /* 20px = 2.5 * 8px */
}

/* Button Container Alignment */
.drop-card-modern .border-t {
    @apply w-full; /* Ensure action bar spans full width */
}

.drop-card-modern .grid,
.drop-card-modern .flex {
    @apply w-full; /* Ensure button containers span full width */
}

/* Line Clamp Utilities - Enhanced Implementation */
.line-clamp-1 {
    display: -webkit-box;
    -webkit-line-clamp: 1;
    -webkit-box-orient: vertical;
    overflow: hidden;
    text-overflow: ellipsis;
}

.line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    text-overflow: ellipsis;
}

/* Enhanced Focus States - WCAG Accessibility */
.btn-modern:focus-visible {
    @apply ring-2 ring-offset-2 outline-none;
    @apply ring-opacity-75;
}

/* Professional Responsive Design - Mobile First */
@media (max-width: 640px) {
    .drop-card-modern {
        @apply mb-6; /* Consistent mobile spacing */
        @apply rounded-lg; /* Slightly smaller radius on mobile */
    }

    .drop-card-modern h3 {
        @apply text-lg leading-6; /* Optimized mobile typography */
        @apply tracking-tight;
    }

    .drop-card-modern .px-6 {
        padding-left: 1rem; /* 16px = 2 * 8px on mobile */
        padding-right: 1rem; /* 16px = 2 * 8px on mobile */
    }

    .btn-modern {
        @apply px-3 py-2; /* Compact mobile buttons */
        @apply text-sm;
        @apply min-h-[44px]; /* Maintain touch targets */
    }
}

/* Tablet Optimizations */
@media (min-width: 641px) and (max-width: 1023px) {
    .drop-card-modern {
        @apply mb-7; /* Tablet spacing */
    }

    .drop-card-modern h3 {
        @apply text-xl leading-7; /* Tablet typography */
    }
}

/* Desktop Enhancements */
@media (min-width: 1024px) {
    .drop-card-modern {
        @apply mb-8; /* Desktop spacing */
    }

    .drop-card-modern:hover {
        @apply shadow-2xl shadow-gray-900/20;
        transform: translateY(-4px) scale(1.01);
    }

    .btn-modern {
        @apply px-5 py-3; /* Larger desktop buttons */
    }
}

/* Professional Accessibility Enhancements */
.drop-card-modern:focus-within {
    @apply ring-2 ring-blue-500 ring-offset-2;
    @apply outline-none;
}

/* High Contrast Mode Support */
@media (prefers-contrast: high) {
    .drop-card-modern {
        @apply border-2 border-gray-900;
    }

    .btn-modern {
        @apply border-2;
    }
}

/* Reduced Motion Support */
@media (prefers-reduced-motion: reduce) {
    .drop-card-modern {
        @apply transition-none;
    }

    .drop-card-modern:hover {
        transform: none;
    }

    .btn-modern {
        @apply transition-none;
    }
}

/* Print Styles */
@media print {
    .drop-card-modern {
        @apply shadow-none border border-gray-400;
        @apply break-inside-avoid;
    }

    .btn-modern {
        @apply border border-gray-400;
    }
}

/* Hover Animations - Subtle and Professional */
.drop-card {
    @apply transform-gpu; /* Hardware acceleration */
}

.drop-card:hover {
    @apply scale-[1.01]; /* Very subtle scale */
}

/* Prevent zoom on double tap for buttons */
.create-drop-btn,
.delete-drop-btn {
    touch-action: manipulation;
    -webkit-touch-callout: none;
    -webkit-user-select: none;
    user-select: none;
}
</style>
